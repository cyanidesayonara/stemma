PySide6>=6.6.0
# DirectML supplies the onnxruntime import package and CPU fallback.
# Do not co-install the conflicting plain onnxruntime distribution.
onnxruntime-directml==1.24.4
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0
lameenc>=1.7.0
# The release lock ships librosa 0.11. librosa 1.0 reworked the phase
# vocoder behind time_stretch/pitch_shift, so CI must not silently test a
# different audio path than the one users get. Lift this cap together with
# the release lock once 1.0 has been checked by ear (issue #154).
librosa>=0.10.1,<1.0
yt-dlp>=2024.1.0
imageio-ffmpeg>=0.4.9
