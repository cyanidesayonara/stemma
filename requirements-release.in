# Regenerate on Windows with Python 3.12:
# .venv\Scripts\python.exe -m piptools compile --allow-unsafe --generate-hashes --output-file=requirements-release.txt requirements-release.in
-r requirements.txt

# Release validation and packaging.
pyinstaller>=6.0
pytest>=8.0.0
pytest-timeout>=2.3.0
ruff>=0.16.0
